
$﻿(document).ready(function() {

  // for configuration look here: http://dotdotdot.frebsite.nl/
  if (jQuery.fn.dotdotdot) {
    $(".ellipsis").dotdotdot({
      after: "a.readmore"
    });
  };

});
